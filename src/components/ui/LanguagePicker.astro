---
import { languages } from '../../i18n/ui'
const { currentLocale } = Astro
const [lang] = Object.keys(languages).filter((lang) => lang != currentLocale)
---

<a
	class="hover:text-ink focus-visible:text-ink transition-colors lg:px-1"
	href={`/${lang}/`}>{lang}</a
>
