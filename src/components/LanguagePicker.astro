---
import { languages } from '../i18n/ui'
const { currentLocale } = Astro
const [lang] = Object.keys(languages).filter((lang) => lang != currentLocale)
---

<a
	class="text-base font-medium uppercase tracking-wide hover:text-stone-800 focus:text-stone-900 md:px-1 hover:dark:text-stone-200 focus:dark:text-stone-100"
	href={`/${lang}/`}>{lang}</a
>
